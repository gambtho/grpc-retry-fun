apiVersion: v1
kind: ConfigMap
metadata:
  name: grpc-retry-fun-config
  namespace: somens
  labels:
    app: grpc-retry-fun
data:
  # Application configuration
  PORT: "50051"
  # Add other configuration values as needed
