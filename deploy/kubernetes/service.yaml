apiVersion: v1
kind: Service
metadata:
  name: grpc-retry-fun
  namespace: 3-6-8pm
  labels:
    app: grpc-retry-fun
spec:
  type: ClusterIP
  selector:
    app: grpc-retry-fun
  ports:
    - name: grpc
      protocol: TCP
      port: 80
      targetPort: 80
