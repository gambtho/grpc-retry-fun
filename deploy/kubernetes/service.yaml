apiVersion: v1
kind: Service
metadata:
  name: grpc-retry-fun
  namespace: yet-another
  labels:
    app: grpc-retry-fun
spec:
  type: ClusterIP
  selector:
    app: grpc-retry-fun
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
  sessionAffinity: None
